# Asset Graph

Asset relationship graph engine for AVA CORE.

## Planned Functionality

- Build a directed dependency graph from asset `dependencies` fields
- Impact analysis: given an alert on asset X, identify which downstream assets are affected
- Expose graph query API (e.g. "what breaks if QF1 trips?")
- Visualisation data for the UI asset map

## Status

🚧 **Planned**.
