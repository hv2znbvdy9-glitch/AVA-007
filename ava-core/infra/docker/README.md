# Docker Compose – AVA CORE (planned)

🚧 **Planned** – Docker Compose setup for running AVA CORE services in containers.

Planned services:
- `api` – REST API
- `ui` – Web dashboard
- `detection-engine`
- `response-engine`
- `asset-graph`
- `twin-mapper`
- `influxdb` – time-series telemetry storage
- `grafana` – visualisation
