# Kubernetes Manifests – AVA CORE (planned)

🚧 **Planned** – Kubernetes deployment manifests for multi-cabinet edge fleet management.

Planned resources:
- `Deployment` for each service
- `DaemonSet` for the edge agent
- `ConfigMap` for configuration
- `Secret` for credentials
- `Service` / `Ingress` for the API and UI
